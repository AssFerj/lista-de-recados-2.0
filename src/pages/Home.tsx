import { AppBar } from '@mui/material';
import React from 'react';

const Home: React.FC = () => {
  return (
    <React.Fragment>
      <AppBar />
    </React.Fragment>
  );
};

export default Home;
