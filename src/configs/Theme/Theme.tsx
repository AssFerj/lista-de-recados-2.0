const themeLight = createMuiTheme({
  palette: {
    background: {
      default: '#e4f0e2'
    }
  }
});

const themeDark = createMuiTheme({
  palette: {
    background: {
      default: '#222222'
    }
  }
});

function createMuiTheme(arg0: { palette: { background: { default: string } } }) {
  throw new Error('Function not implemented.');
}
